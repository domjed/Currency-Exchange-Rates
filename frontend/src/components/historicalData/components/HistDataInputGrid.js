import { Grid } from "@mui/material/";
import { styled } from "@mui/material/styles";

export const HistDataInputGrid = styled(Grid)({
    display: 'flex',
    width: 600,
    margin: 'auto',
    border: '1px solid',
    borderRadius: 10,
    fontSize: '0.875rem',
    fontWeight: '700',
    padding: 30,
    marginTop: 10,
    marginBottom: 30,
});