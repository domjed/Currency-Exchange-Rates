import { styled } from "@mui/material/styles";
import { Grid } from "@mui/material/";

export const CurrentDataInputGrid = styled(Grid)({
    display: 'flex',
    width: 800,
    margin: 'auto',
    border: '1px solid',
    borderRadius: 10,
    fontSize: '0.875rem',
    fontWeight: '700',
    padding: 30,
    marginTop: 10,
    marginBottom: 30,
});